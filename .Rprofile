#source("renv/activate.R")

# packages not found after source("renv/activate.R") ...
# Local cheem build causing file to not work; used
# renv::settings$ignored.packages("cheem")
## May need to install cheem to replicate cheem figures:
# install.packages("cheem") ## cran
# remotes::
